AHORA: 
- Bug en update_stock??? 

SIGUE:
- CAMBIO DE PRECIOS: QUE NO AFECTE PREVIAS COMPRAS - VENTAS NI ESTADISTICAS
  - child model of product/supply, with price history
- Generate_total method (inflow) call on controller to set value -- after_save?
- Notifications
	- Bootstrap
		- Simple Forms: Notifications: force notifications_path
		- Index: 3 tables, ordered by: lower stock, lower balance, closer due date.
	- AutoNotifications
		- Calls on controller
		- Create tables?
	- Manual Notifications
- Scopes
  - Limitar index a últimas n entradas para evitar carga lenta, dar opción a cargar de a n cantidad por vez
			index method within controller, Project.all  -x-> custom query
	- Agregar scopes manuales a statistics y take
	- Auto eliminación de Notificaciones
- Paginación

	Gamuza: Guardar una variable en un hidden field para que cada uno de los renders tenga un id unico (la otra es poner el horario)
	En el onclick sumarle una llamada al js de select2 para ESE (this) select rendereado

- JavaScript
	- Calcular total y subtotal automáticamente
	- Select con autocomplete, javascript
	- Delete function from forms doesn't actually delete stuff
	- Cash boolean doesn't save properly
- Devise
		- Dos usuarios: Admin-Sandra (acceso a todas las secciones)
										La Raquelita (acceso sólo a inflows y outflows)
- I18n
		- Dry out the file: dictionary of repeated terms
				dictionary:
				  email: &email Email
				  name: &name Name
				  password: &password Password
				  confirmation: &confirmation Confirmation

				activerecord:
				  attributes:
				    user:
				      email: *email
				      name: *name
				      password: *password
				      password_confirmation: *confirmation
		- Traducción a español
