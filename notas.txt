Notas

- Mass stock update bug "no implicit conversion of Symbol into Integer"
	cómo generar un Simple Form con multiple fields que devuelva como array de Hashes.

 	products = product_params[:mass_stock] --> Esto tiene que ser un array de Hashes
    products.each do |parameters| ---> para poder hacer .each sobre el array
      unless parameters[:product_id].empty? || parameters[:stock].empty?
        product = Product.find(parameters[:product_id].to_i)
        product.update_stock(parameters[:stock].to_i)
      end
    end

  maybe: write the data in controller or model method as a Hash? so the simple form params end up inside a hash? and therefore multiple inputs become an array of hashes?


- Bootstrap
	- Simple Forms: inflows, outflows, mass stock update
	Inside your views, use the 'simple_form_for' with the Bootstrap form
  class, '.form-inline', as the following:
	simple_form_for(@user, html: { class: 'form-inline' }) do |f|
- Notifications
	- Types: low stock - balance alert - client orders
	- auto generation (ls - ba)
	- create method in controller for client orders
	- 3 tables for index, one per type OR a subtype for user-generated notifications
- Scopes
    - Limitar index a últimas n entradas para evitar carga lenta, dar opción a cargar de a n cantidad por vez
				index method within controller, Project.all  -x-> custom query
		- Agregar scopes manuales a statistics y take
		- Auto eliminación de Notificaciones

-----------------------------------------------------------------------------------------------------------------------------------------

- JavaScript
	- Calcular total y subtotal automáticamente
	- Select con autocomplete, javascript
	- Delete function from forms doesn't actually delete stuff
	- Cash boolean doesn't save properly
- I18n
		- Dry out the file: dictionary of repeated terms
				dictionary:
				  email: &email Email
				  name: &name Name
				  password: &password Password
				  confirmation: &confirmation Confirmation

				activerecord:
				  attributes:
				    user:
				      email: *email
				      name: *name
				      password: *password
				      password_confirmation: *confirmation
		- Traducción a español
- Devise
		- Dos usuarios: Admin-Sandra (acceso a todas las secciones)
										La Raquelita (acceso sólo a inflows y outflows)
- GitHub portfolio: config git.ignore
