<p id="notice"><%= notice %></p>

<h1><%= I18n.t('activerecord.models.supply_product_link.other') %></h1>

<table>
  <thead>
    <tr>
      <th><%= I18n.t('activerecord.attributes.supply_product_links.product_id') %></th>
      <th><%= I18n.t('activerecord.attributes.supply_product_links.supply_id') %></th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @supply_product_links.each do |supply_product_link| %>
      <tr>
        <td><%= supply_product_links.product_id %></td>
        <td><%= supply_product_links.supply_id %></td>
        <td><%= link_to I18n.t('helpers.buttons.destroy'), supply_product_links, method: :delete, data: { confirm: I18n.t('helpers.buttons.are_you_sure') } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to I18n.t('helpers.links.new_supply_product_link'), new_supply_product_link_path %>