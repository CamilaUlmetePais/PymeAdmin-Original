<% if klass.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(klass.errors.count, "error") %> prohibited this <%= klass.name %> from being saved:</h2>

    <ul>
    <% klass.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>